public class VarDemo {
    //var a = 1; => var은 메서드 내부에서만 사용 가능
    public static void main(String[] args) {
        int var = 1; //var을 변수 이름으로 사용 가능
        var x = 1;

        //var x = 1, y = 3, z = 4; //var을 사용하여 다수의 변수를 하나의 실행문으로 초기화 가능

        //var str = null; => var 변수에 null을 대입할 수 없음

        //var oops;
        //oops = 1; // var 변수를 바로 초기화하지 않으면 타입 추론 불가

    }
}
