package chap02;

public class VarDemo {

    public static void main(String[] args) {
        var a = 1;
        var x = 1;

        // var x=1, y=1;  -> var type은 2개이상 동시 초기화 불가
        // var str = null;

        //var oops;
        //oops = 1; -> var 사용시에 동시에 초기화해줘야


    }

    // void test(var x) -> 함수 인수로는 var 사용 불가능.
}